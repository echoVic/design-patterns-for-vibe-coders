# 章节目录

**初稿完成，经五轮独立审读修订。**

| | |
|---|---|
| 篇幅 | 17 章 + 2 附录，约 4.4 万字 |
| 案例 | `case/` 下五个版本，v0.1(44 行) → v1.1(237 行) |
| 插图 | 封面 + 8 张 |
| 风格 | 各章破折号 ≤6、加粗 ≤6、感叹号 0、emoji 0 |

## 00　为什么还要讲设计模式

从一段真实的 AI 生成代码开始：让模型写一个速记应用，44 行能用；说一句「重构得专业点」，出来七个文件 150 行。然后问——**这些抽象哪些是必要的？**

中途发现重构弄丢了 HTML 转义。这一章定全书的调性：不是教你造，是教你拆。

## 第一部分　先学会读

| 章 | 回答什么 |
|---|---|
| 01 | 拿到一份代码，先数三个数：操作数、层数、要打开几个文件 |
| 02 | 抽象的三笔开销：跳转、改动面、读代码时的默认假设 |
| 03 | 怎么把一段陌生代码画成一张调用图，找出直通节点 |

## 第二部分　你会遇到什么 ★核心

| 章 | 场景 | 结论 |
|---|---|---|
| 04 | 这个接口只有一个实现 | 誊本还是抽象，看它比实现窄不窄 |
| 05 | 传函数，还是传对象 | 一个只有单方法的接口就是一个函数类型 |
| 06 | if-else 越写越长 | 分支变数据；AI 给对了方向但给多了 107 行 |
| 07 | 构造函数参数越来越多 | 分组；AI 同样给对了方向，但做了 1186 行 |
| 08 | 包了一层又一层 | 包装什么时候该留、哪一层该摘 |
| 09 | 状态变了，界面没更新 | 先治重复状态，再谈订阅 |
| 10 | 到处都在 new | 一个组合根，不要容器 |
| 11 | 加一个功能要动几个文件 | 同类改动和新维度要分开判 |
| 12 | 同样的代码出现了三遍 | 抽出来看参数，开关多就是假重复 |
| 13 | 谁都需要那个对象 | 环境和数据要分开处理（这一章是推演，案例里还没发生） |
| 14 | 什么时候该拆 | 四步判断：量成本、数消费者、看参数、列清单 |
| 15 | 怎么安全地拆 | 内联，不是重新设计 |
| 16 | 抽象的收益曲线 | 收尾；终点版一度漏了转义，发现、演示、修好 |

## 附录

- **A　模式索引** —— 按名字反查章节。只列正文真的当术语讲过的 11 个名字。
- **B　一页检查清单** —— 可以打印出来贴在显示器上。

**附录 B 是风格指标的例外。** 那是一页清单，加粗在承担结构作用（每个检查项就是一句加粗的问句），不是散文里的强调。

## 写作约定

- 读者是非计算机专业，术语首次出现当场定义，不用 UML
- 每章末尾给「检查句」，比如：看到一个只有一个实现的接口，问一句**第二个什么时候来？**
- 每章可以有一节「谬误与陷阱」，列读者最可能判断错的地方
- **书中所有数字都能在 `case/` 里复核**，`node scripts/check-claims.mjs` 会核对
